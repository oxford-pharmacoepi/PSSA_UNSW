# Results

Study outputs are written here and are ignored by Git, except for this README.
Summarised-result CSVs are written to `summarised_result/` before being imported
with `omopgenerics::importSummarisedResult()`.
